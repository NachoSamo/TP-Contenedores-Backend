spring.application.name=TP-Contenedores-Backend

# Google Maps API configuration
# Set your API key here or via environment variable (e.g. in docker-compose set GOOGLE_MAPS_API_KEY)
google.maps.api.key=
# Geocoding endpoint (you normally don't need to change this)
google.maps.geocode.url=https://maps.googleapis.com/maps/api/geocode/json
